#  Go-lang-Vcs-NEXTClan 

coverage:
  status:
    project:
      default:
        target: 85%
    patch:
      default:
        target: 85%
        only_pulls: true

ignore:
  - "test/bdd"  # ignore bdd tests
  - "component/walletcli"  # ignore walletcli testing tool
